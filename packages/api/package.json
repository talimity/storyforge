{
  "name": "@storyforge/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check": "run-p --continue-on-error lint typecheck",
    "clean": "rm -rf dist",
    "fmt": "biome format --write .",
    "lint": "biome check --diagnostic-level error --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@storyforge/shared": "workspace:*",
    "@trpc/server": "^11.0.0-rc.676",
    "fastify": "^5.1.0",
    "trpc-to-openapi": "^2.0.0-rc.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
