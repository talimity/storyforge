Follow the following todo list and check them off as you go:
1. Remove superfluous comments
2. Run: `pnpm typecheck` and fix any issues
3. Run: `pnpm lint` and fix any issues that eslint cannot auto-fix
4. Run: `pnpm format` to apply Prettier
5. Create a new branch if we're currently on master
6. Commit
  - Use conventional commit messages. Include a scope if applicable.
